Project for test demonstration Hibernate

Project works with PostgreSQL database. Run the following commands to deploy database to Docker container 

make build 
make run

After that database will expose on 5432 port. 